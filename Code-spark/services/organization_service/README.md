# Organization Service

This service manages organizations, courses, and recruitments.

## Setup

1.  Install dependencies:
    ```bash
    npm install
    ```

2.  Set up the database by running the `db/setup.sql` script.

3.  Create the database tables by running the migrations.

## Running the service

```bash
npm start
```
