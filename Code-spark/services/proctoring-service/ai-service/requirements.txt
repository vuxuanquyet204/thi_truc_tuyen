fastapi
uvicorn[standard]
python-socketio[asyncio_client]
python-multipart
opencv-python
numpy
mediapipe
ultralytics>=8.0.0
scikit-learn

# ==================== GPU SUPPORT ====================
# Install PyTorch with CUDA (choose ONE based on your GPU):

# Option 1: NVIDIA RTX 30/40/50 series (Ada Lovelace, Ampere, Blackwell)
#   CUDA 12.1 - Recommended for RTX 3060 and newer
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# Option 2: NVIDIA RTX 20/16 series (Turing)
#   CUDA 11.8 - For RTX 2060, 2070, 2080, GTX 16xx
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# Option 3: CPU only (no GPU)
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# Verify GPU: python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0))"
# Expected: True NVIDIA GeForce RTX ...

# TensorRT (optional - 2-3x faster than vanilla PyTorch):
# pip install tensorrt (requires CUDA 12.x + matching tensorrt version)
