<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.0</version>
        <relativePath/>
    </parent>

    <groupId>com.codespark</groupId>
    <artifactId>code_spark</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>CodeSpark Parent</name>
    <description>Parent POM for CodeSpark microservices platform</description>

    <modules>
        <module>services/common-library</module>
        <module>services/discovery-service</module>
        <module>services/config-server</module>
        <module>services/api-gateway</module>
        <module>services/identity-service</module>
        <module>services/profile-service</module>
        <module>services/file-service</module>
        <module>services/notification-service</module>
        <module>services/exam-service</module>
        <module>services/analytics-service</module>
        <module>services/course-service</module>
        <module>services/user-service</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <spring-cloud.version>2023.0.1</spring-cloud.version>
        <!-- Version định nghĩa tập trung -->
        <lombok.version>1.18.34</lombok.version>
        <mapstruct.version>1.5.5.Final</mapstruct.version>
        <springdoc.version>2.5.0</springdoc.version>
        <commons-lang3.version>3.12.0</commons-lang3.version>
        <jjwt.version>0.11.5</jjwt.version>
        <zxing.version>3.5.3</zxing.version>
        <aws-sdk.version>2.20.26</aws-sdk.version>
        <hypersistence-utils.version>3.7.0</hypersistence-utils.version>
        <poi.version>5.2.5</poi.version>
        <bucket4j.version>8.7.0</bucket4j.version>
        <cloudinary.version>1.37.0</cloudinary.version>
        <spring-ai.version>1.0.0-M4</spring-ai.version>
        <yubico.version>2.5.3</yubico.version>
        <dotenv.version>4.0.0</dotenv.version>

        <!-- ==================== JVM TUNING ==================== -->
        <!-- JVM Memory Settings (recommended heap sizes) -->
        <jvm.xms.discovery>256m</jvm.xms.discovery>
        <jvm.xmx.discovery>384m</jvm.xmx.discovery>
        <jvm.xms.config>256m</jvm.xms.config>
        <jvm.xmx.config>384m</jvm.xmx.config>
        <jvm.xms.gateway>384m</jvm.xms.gateway>
        <jvm.xmx.gateway>512m</jvm.xmx.gateway>
        <jvm.xms.identity>512m</jvm.xms.identity>
        <jvm.xmx.identity>1024m</jvm.xmx.identity>
        <jvm.xms.profile>512m</jvm.xms.profile>
        <jvm.xmx.profile>768m</jvm.xmx.profile>
        <jvm.xms.user>384m</jvm.xms.user>
        <jvm.xmx.user>512m</jvm.xmx.user>
        <jvm.xms.course>512m</jvm.xms.course>
        <jvm.xmx.course>1024m</jvm.xmx.course>
        <jvm.xms.exam>768m</jvm.xms.exam>
        <jvm.xmx.exam>1536m</jvm.xmx.exam>
        <jvm.xms.file>384m</jvm.xms.file>
        <jvm.xmx.file>512m</jvm.xmx.file>
        <jvm.xms.notification>256m</jvm.xms.notification>
        <jvm.xmx.notification>384m</jvm.xmx.notification>
        <jvm.xms.analytics>512m</jvm.xms.analytics>
        <jvm.xmx.analytics>768m</jvm.xmx.analytics>
        <jvm.xms.admin>256m</jvm.xms.admin>
        <jvm.xmx.admin>384m</jvm.xmx.admin>
    </properties>

    <!-- ============================================================
         DEPENDENCY MANAGEMENT - Tất cả version khai báo TẬP TRUNG tại đây
         Các service con chỉ cần khai báo <dependency> mà KHÔNG cần <version>
         ============================================================ -->
    <dependencyManagement>
        <dependencies>
            <!-- ==================== BUILD TOOLS ==================== -->
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct-processor</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>

            <!-- ==================== SPRING CLOUD - khai báo version tay (không dùng BOM import) ==================== -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>
                <version>4.1.3</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-starter-netflix-eureka-server</artifactId>
                <version>4.1.3</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-config-server</artifactId>
                <version>4.0.3</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-starter-gateway</artifactId>
                <version>4.1.3</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-starter-openfeign</artifactId>
                <version>4.1.3</version>
            </dependency>

            <!-- ==================== DATABASE ==================== -->
            <dependency>
                <groupId>org.postgresql</groupId>
                <artifactId>postgresql</artifactId>
                <version>42.7.3</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>io.hypersistence</groupId>
                <artifactId>hypersistence-utils-hibernate-63</artifactId>
                <version>${hypersistence-utils.version}</version>
            </dependency>
            <dependency>
                <groupId>jakarta.persistence</groupId>
                <artifactId>jakarta.persistence-api</artifactId>
                <version>3.1.0</version>
            </dependency>

            <!-- ==================== OPENAPI / SWAGGER ==================== -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webflux-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>

            <!-- ==================== UTILS ==================== -->
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>${commons-lang3.version}</version>
            </dependency>
            <dependency>
                <groupId>javax.validation</groupId>
                <artifactId>validation-api</artifactId>
                <version>2.0.1.Final</version>
            </dependency>
            <dependency>
                <groupId>me.paulschwarz</groupId>
                <artifactId>spring-dotenv</artifactId>
                <version>${dotenv.version}</version>
            </dependency>

            <!-- ==================== JWT ==================== -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>

            <!-- ==================== REDIS ==================== -->
            <dependency>
                <groupId>redis.clients</groupId>
                <artifactId>jedis</artifactId>
                <version>5.1.0</version>
            </dependency>

            <!-- ==================== MONITORING ==================== -->
            <dependency>
                <groupId>io.micrometer</groupId>
                <artifactId>micrometer-registry-prometheus</artifactId>
                <version>1.12.7</version>
                <scope>runtime</scope>
            </dependency>

            <!-- ==================== AWS / S3 / MinIO ==================== -->
            <dependency>
                <groupId>software.amazon.awssdk</groupId>
                <artifactId>s3</artifactId>
                <version>${aws-sdk.version}</version>
            </dependency>

            <!-- ==================== SECURITY / FIDO2 ==================== -->
            <dependency>
                <groupId>com.yubico</groupId>
                <artifactId>webauthn-server-core</artifactId>
                <version>${yubico.version}</version>
            </dependency>
            <dependency>
                <groupId>com.yubico</groupId>
                <artifactId>webauthn-server-attestation</artifactId>
                <version>${yubico.version}</version>
            </dependency>

            <!-- ==================== QR CODE ==================== -->
            <dependency>
                <groupId>com.google.zxing</groupId>
                <artifactId>core</artifactId>
                <version>${zxing.version}</version>
            </dependency>
            <dependency>
                <groupId>com.google.zxing</groupId>
                <artifactId>javase</artifactId>
                <version>${zxing.version}</version>
            </dependency>

            <!-- ==================== FILE PROCESSING ==================== -->
            <dependency>
                <groupId>org.apache.poi</groupId>
                <artifactId>poi-ooxml</artifactId>
                <version>${poi.version}</version>
            </dependency>
            <dependency>
                <groupId>com.cloudinary</groupId>
                <artifactId>cloudinary-http44</artifactId>
                <version>${cloudinary.version}</version>
            </dependency>

            <!-- ==================== RATE LIMITING ==================== -->
            <dependency>
                <groupId>com.bucket4j</groupId>
                <artifactId>bucket4j-core</artifactId>
                <version>${bucket4j.version}</version>
            </dependency>

            <!-- ==================== AI ==================== -->
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-openai-spring-boot-starter</artifactId>
                <version>${spring-ai.version}</version>
            </dependency>

            <!-- ==================== COMMON LIBRARY ==================== -->
            <dependency>
                <groupId>com.dao</groupId>
                <artifactId>common-library</artifactId>
                <version>${project.version}</version>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <!-- ============================================================
         BUILD PLUGIN MANAGEMENT - Quản lý plugin version tập trung
         ============================================================ -->
    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.11.0</version>
                    <configuration>
                        <release>${java.version}</release>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                            <path>
                                <groupId>org.mapstruct</groupId>
                                <artifactId>mapstruct-processor</artifactId>
                                <version>${mapstruct.version}</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <configuration>
                        <excludes>
                            <exclude>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                            </exclude>
                        </excludes>
                        <!-- JVM Tuning Arguments - sẽ được override trong từng service -->
                        <jvmArguments>
                            -XX:+UseZGC
                            -XX:MaxGCPauseMillis=100
                            -XX:+HeapDumpOnOutOfMemoryError
                            -XX:HeapDumpPath=/var/log/heapdump.hprof
                            -Djava.security.egd=file:/dev/./urandom
                        </jvmArguments>
                    </configuration>
                </plugin>
                <!-- JVM Tuning Plugin - Enable qua profile 'jvm-tuning' -->
                <plugin>
                    <groupId>com.bazaarvoice.maven.plugins</groupId>
                    <artifactId>process-exec-maven-plugin</artifactId>
                    <version>1.0.0</version>
                    <inherited>false</inherited>
                    <configuration>
                        <name>java</name>
                        <spawn>${spawn}</spawn>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>
